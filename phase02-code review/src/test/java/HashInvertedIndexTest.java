import org.junit.Assert;
import org.junit.Test;


public class HashInvertedIndexTest {
    /*private final HashInvertedIndex hashInvertedIndex = new HashInvertedIndex();

    @Test
    public void shouldOrganizeData(){
        hashInvertedIndex.organizeDocsAndWords();
        Assert.assertTrue(hashInvertedIndex.allWordsContain("hello"));
        Assert.assertFalse(hashInvertedIndex.allWordsContain("mechanic"));
        Assert.assertEquals(16, hashInvertedIndex.getDocsContain("hello").size());
    }

    @Test
    public void shouldReturnObject() {
        Assert.assertNotNull(HashInvertedIndex.getInstance());
    }*/
}